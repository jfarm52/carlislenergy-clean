# Local environment overrides (DO NOT COMMIT SECRETS)
# Copy this file to `local.env` and fill in values.
#
# The app loads (in order): local.env, .env.local, then .env

# --- Google Places (Customer Name autocomplete / nearby businesses) ---
GOOGLE_PLACES_API_KEY=

# --- Utility Bills / Bill Intake (PostgreSQL required) ---
# If using docker-compose.yml from this repo:
# DATABASE_URL=postgresql://carlisle:carlisle@localhost:5432/carlisle_bills
DATABASE_URL=

# --- xAI Grok (bill extraction) ---
XAI_API_KEY=


